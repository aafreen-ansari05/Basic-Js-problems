/**
 * factorial of number 
 */
                            //empty Array  
let f =1;
for (let txt= 5;txt>=1;txt -= 1){
   f = f * txt;                       //pushing elements into array
}

console.error(f)