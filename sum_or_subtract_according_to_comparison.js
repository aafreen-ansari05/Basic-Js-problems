/**
 * 
 * @param {Int} a 
 * @param {Int} b 
 * if a is greater than b then function returns (a-b) otherwise, it will return (a+b)----
 * a and b should be Number or integer----
 */
function sumSubtact(a,b){
    if(a>b){
        console.log(a-b);
    }
    else{
    console.log(a+b);
    }
}
let f = sumSubtact(40,20);
